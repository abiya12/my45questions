interface item{
    name:string;
    price:number;
}

let car: item={
    name: "BMW",
    price: 9500000
}
let bike:item={
    name: "Honda",
    price:650000
}

console.log(`car name:${car.name} & car price: ${car.price}`)
console.log(`car name:${car.name} & car price: ${car.price}`)